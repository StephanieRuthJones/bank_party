# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem "pry", "~> 0.12.2"
#require_all is tool that helps us pull in files from a directory so we don't have to import files individually
gem "require_all", "~> 3.0"
